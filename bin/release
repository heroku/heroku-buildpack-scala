#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
config_vars:
  PATH: .opt/bin:.sbt:/usr/local/bin:/usr/bin:/bin
  JAVA_OPTS: -Xmx384m -Xss256k -XX:+UseCompressedOops
  SBT_OPTS: -Xmx384m -Xss256k -XX:+UseCompressedOops
  REPO: /app/.ivy2/cache
addons:
  shared-database:5mb

EOF
