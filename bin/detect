#!/usr/bin/env bash
# bin/use <build-dir>

cd $1

## SBT 0.10 allows either *.sbt in the root dir, or project/*.scala or .sbt/*.scala
if detect_sbt . ; then
  if is_play20 . ; then
    echo "Play 2.0" && exit 0
  else
    echo "Scala" && exit 0
  fi
else
  echo "no" && exit 1
fi
